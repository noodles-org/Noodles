apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: secure-redirect
  namespace: foundry
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: route
  namespace: foundry
  labels:
    noodles.dashboard/service: "true"
  annotations:
    noodles.dashboard/name: "Foundry"
    noodles.dashboard/description: "Foundry VTT game server"
    noodles.dashboard/category: "Applications"
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`foundry.noodles.quest`)
      kind: Rule
      services:
        - name: traefik-service
          namespace: foundry
          kind: TraefikService
      middlewares:
        - name: secure-redirect
  tls:
    secretName: noodles-quest-prod-tls