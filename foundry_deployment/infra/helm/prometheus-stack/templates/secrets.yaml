{{- $files := .Files }}
{{- $path := "../../../k8s/monitoring/.secret.yaml"}}
{{- $secretData := $files.Get $path | fromYaml }}
apiVersion: v1
kind: Secret
metadata:
  name: github
  namespace: monitoring
type: Opaque
data:
{{- range $name, $secret := ($secretData) }}
  {{ $name }}: {{ $secret | b64enc }}
{{- end }}
