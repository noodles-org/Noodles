apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: secure-redirect
  namespace: argocd
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`argocd.noodles.quest`)
      priority: 10
      services:
        - name: argocd-server
          port: 80
      middlewares:
        - name: secure-redirect
    - kind: Rule
      match: Host(`argocd.noodles.quest`) && Header(`Content-Type`, `application/grpc`)
      priority: 11
      services:
        - name: argocd-server
          port: 80
          scheme: h2c
      middlewares:
        - name: secure-redirect
  tls:
    secretName: noodles-quest-prod-tls